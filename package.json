{
  "name": "fotmob-mcp",
  "version": "0.1.0",
  "description": "FotMob data tools for MCP clients",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --check src/fotmob.js && node --check src/index.js && node scripts/build.mjs",
    "check": "npm test && npm run build",
    "start": "node dist/index.js",
    "dev": "node src/index.js",
    "test": "node --test test/*.test.js"
  }
}
